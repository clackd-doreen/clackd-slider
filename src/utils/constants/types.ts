const alignments = [
	'center',
	'left',
	'right',
] as const

const componentSizes = [
	'xs',
	'sm',
	'md',
	'lg',
	'xl',
] as const

const directions = [
	'down',
	'left',
	'right',
	'up',
] as const

const orientations = [
	'horizontal',
	'vertical',
] as const

const priorities = [
	'low',
	'medium',
	'high',
] as const

const rotations = [
	'clockwise',
	'counterclockwise',
] as const

const shapes = [
	'circle',
	'diamond',
	'rectangle',
	'square',
] as const


export {
	alignments,
	componentSizes,
	directions,
	orientations,
	priorities,
	rotations,
	shapes,
}
